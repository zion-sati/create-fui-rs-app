# __PROJECT_NAME__

FUI-RS routed MVC scaffold.

Each route is a separately built WASM package. Route crates use `fui_managed_app!`, so the app lifecycle ABI exports are generated by SDK glue instead of handwritten in app code.

```sh
npm install
npm run build
npm run dev
```
